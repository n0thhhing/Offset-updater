{
  "name": "offset_updater",
  "version": "1.0.0",
  "description": "This script is designed to find offsets in a new library based on a set of offsets and a pattern from an old library. The algorithm optimizes the search process by skipping unnecessary iterations, resulting in improved performance.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.{js,json}\"",
    "start": "uglifyjs index.js -o dist/index.js && node dist/index.js",
    "min": "uglifyjs index.js -o dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0",
    "hex2dec": "^1.1.2",
    "hexoid": "^1.0.0",
    "patternfinder": "^0.0.1",
    "read-chunk": "^4.0.3",
    "readline-sync": "^1.4.10"
  }
}
